<!DOCTYPE html>
<html lang="en">
<head>
  <%- include("./partials/head") %>
  <title>404 - Zipply</title>
</head>
<body class="d-flex flex-column min-vh-100">
  <%- include("./partials/nav") %>

  <main class="container text-center my-5 flex-grow-1">
    <h1 class="display-4 text-danger mb-3"> <i class="bi bi-exclamation-triangle-fill me-2"></i>404 - Not Found</h1>

    <% if (typeof message !== "undefined") { %>
      <p class="lead"><%= message %></p>
    <% } else { %>
      <p class="lead">The short URL you are trying to access does not exist, has expired, or was deleted.</p>
    <% } %>

    <a href="/" class="btn btn-outline-primary mt-4">← Go Back Home</a>
    <div style="height: 20vh;"></div>
  </main>

  <%- include("./partials/footer") %>
  <%- include("./partials/scripts") %>
</body>
</html>
